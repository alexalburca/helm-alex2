apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alex.fullname" . }}-test-connection"
  labels:
    {{- include "alex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
