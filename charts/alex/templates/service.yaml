apiVersion: v1
kind: Service
metadata:
  name: {{ include "alex.fullname" . }}
  labels:
    {{- include "alex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alex.selectorLabels" . | nindent 4 }}
